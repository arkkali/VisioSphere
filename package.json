{
  "name": "visiosphere",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "cd backend && npm ci --omit=dev"
  },
  "engines": {
    "node": "22.x"
  }
}
